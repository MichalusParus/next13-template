export enum routes {
  home = '/',
  about = '/about',
  login = '/user/login',
  signup = '/user/sign-up',
  search = '/search',
}

export enum privateRoutes {
  components = '/components',
  tasks = '/tasks',
  profile = '/user/profile',
}

export enum adminRoutes {
  admin = '/user/admin',
}
